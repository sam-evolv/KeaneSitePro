Objective: Fine-tune the existing Key Insight Services site to a premium, enterprise finish. Do not change layout, sections, copy, routing, or assets. Only polish styles, states, accessibility, and performance. Avoid breaking changes.
0) Guardrails
Do not move sections, rename files, or change URLs.
Limit edits to CSS, minor HTML attributes (ARIA/alt/rel), and lightweight JS enhancements.
If a rule conflicts, prefer the site’s current look and adjust minimally.
1) Global visual polish (styles.css)
Create a design token block and use it consistently:
:root{
  --orange:#F15A29; --black:#0B0B0B; --charcoal:#141414; --muted:#B9B9B9; --white:#fff;
  --radius-1:10px; --radius-2:14px; --radius-pill:999px;
  --shadow-1:0 6px 18px rgba(0,0,0,.25);
  --shadow-2:0 10px 28px rgba(0,0,0,.35);
  --border-1:1px solid rgba(255,255,255,.08);
  --transition:.22s cubic-bezier(.2,.8,.2,1);
}
Typography rhythm: tighten line-height on large headings and normalize section spacing.
h1{line-height:1.06; letter-spacing:.1px}
h2{line-height:1.12}
section{padding-block: clamp(44px,6vw,72px)}
.container{max-width:1100px;margin-inline:auto;padding-inline:20px}
Buttons: unify primary/ghost, add premium hover/focus (subtle lift + ring).
.btn{border-radius:var(--radius-1); padding:.78rem 1.05rem; font-weight:700;
     border:1px solid transparent; transition:transform var(--transition), box-shadow var(--transition), border-color var(--transition)}
.btn--primary{background:var(--orange);color:#111; box-shadow:var(--shadow-1)}
.btn--primary:hover{transform:translateY(-1px); box-shadow:var(--shadow-2)}
.btn--ghost{background:transparent;color:#fff;border-color:rgba(255,255,255,.5)}
.btn:focus-visible{outline:0; box-shadow:0 0 0 3px rgba(241,90,41,.45)}
Cards (services/aside): unify corners, borders, and hover.
.card, .svc-card, .strength{border:var(--border-1); border-radius:var(--radius-2);
  background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.02)); transition:transform var(--transition), box-shadow var(--transition), border-color var(--transition)}
.card:hover, .svc-card:hover, .strength:hover{transform:translateY(-2px); box-shadow:var(--shadow-1); border-color:rgba(241,90,41,.35)}
.card__more{font-weight:700; text-decoration:none; color:var(--orange)}
.card__more:hover{text-decoration:underline}
2) Header polish (keep transparent-on-top behavior)
Ensure transparent at top, dark on scroll (retain existing JS). Smooth link color/opacity:
.site-header{background:transparent; transition:background .25s ease, box-shadow .25s ease}
.site-header.scrolled{background:rgba(0,0,0,.85); backdrop-filter:saturate(140%) blur(8px); box-shadow:var(--shadow-1); border-bottom:var(--border-1)}
.nav a{color:#fff; opacity:.92; transition:opacity var(--transition)}
.nav a:hover{opacity:1}
3) Hero refinements (no layout change)
Keep video full-bleed; ensure legibility with overlay and sub-pixel seam fix:
.hero{min-height:100dvh; position:relative; overflow:hidden; display:grid; place-items:center}
.hero__media{position:absolute; inset:-1px; z-index:0}
.hero-video{position:absolute; inset:0; width:100%; height:100%; object-fit:cover; object-position:center; display:block; background:#000}
.hero__overlay{position:absolute; inset:0; background:linear-gradient(180deg,rgba(0,0,0,.35),rgba(0,0,0,.58)); z-index:1}
.hero__content{position:relative; z-index:2; text-align:center}
Subtle fade-in on load for hero copy (respect reduced motion):
.fade-in{opacity:0; transform:translateY(6px); animation:fadeIn .5s var(--transition) forwards}
@keyframes fadeIn{to{opacity:1; transform:none}}
@media (prefers-reduced-motion:reduce){ .fade-in{animation:none; opacity:1; transform:none} }
4) Service pages cohesion (no structure change)
Harmonize breadcrumb style + spacing:
.breadcrumb{display:flex; gap:8px; align-items:center; color:#C9C9C9; font-size:14px}
.breadcrumb a{color:#fff; text-decoration:none; opacity:.9}
.breadcrumb a:hover{opacity:1; text-decoration:underline}
.svc-hero{padding-top:calc(24px + var(--header-h)); background:#111; color:#fff}
.svc-title{margin:.3rem 0 .25rem; font-size:clamp(28px,4vw,42px)}
Normalize lists and step flows:
.svc-list, .svc-steps{margin:0 0 12px 18px}
.svc-steps li{margin:.2rem 0}
5) Forms: enterprise-grade UX (no layout move)
Consistent inputs, placeholders, error/focus styles:
.form input, .form select, .form textarea{
  width:100%; border-radius:var(--radius-1); border:1px solid rgba(255,255,255,.18);
  background:rgba(255,255,255,.04); color:#fff; padding:.7rem .8rem; transition:border-color var(--transition), box-shadow var(--transition), background var(--transition)
}
.form input::placeholder, .form textarea::placeholder{color:#9A9A9A}
.form input:focus, .form select:focus, .form textarea:focus{
  outline:0; border-color:rgba(241,90,41,.55); box-shadow:0 0 0 3px rgba(241,90,41,.25)
}
select{appearance:none; background-image:linear-gradient(45deg,transparent 50%, #fff 50%), linear-gradient(135deg,#fff 50%, transparent 50%);
       background-position:calc(100% - 18px) calc(1.05em), calc(100% - 13px) calc(1.05em);
       background-size:5px 5px, 5px 5px; background-repeat:no-repeat}
.form .help{font-size:.85rem; color:var(--muted)}
Netlify success/failure inline message (non-blocking).
6) Accessibility & semantics (lightweight only)
Add a skip link at top of body:
<a class="skip-link" href="#main">Skip to content</a>
.skip-link{position:absolute;left:-9999px;top:auto}
.skip-link:focus{left:20px;top:14px; background:#000; color:#fff; padding:.5rem .7rem; border-radius:var(--radius-1); z-index:1100}
Ensure every <img> has meaningful alt, and decorative images use alt="" and aria-hidden="true".
Maintain heading order (one H1 per page).
7) Micro-interactions
Subtle section dividers only:
.section-divider{height:1px; background:linear-gradient(90deg,transparent, rgba(255,255,255,.08), transparent); margin:28px 0}
Link underline animation (brand-classy, not flashy):
a.link-underline{position:relative; text-decoration:none}
a.link-underline::after{content:""; position:absolute; left:0; right:100%; bottom:-2px; height:2px; background:var(--orange); transition:right var(--transition)}
a.link-underline:hover::after{right:0}
8) Performance hygiene (no build tools)
Add to <head>:
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<meta name="theme-color" content="#111111">
Ensure scripts are defer and avoid CLS by setting explicit width/height on logos and images where easy.
Lazy-load non-critical images: loading="lazy".
9) Metadata & sharing
Standardize OG/Twitter tags on all pages (reuse title/description pattern).
Add rel="noopener noreferrer" to any external links.
10) QA / Acceptance checklist (must pass before finishing)
Header transparent at top; transitions to dark on scroll; no white/grey bars.
Hero video fully covers viewport; no 1-px seams top/bottom.
Buttons and cards share consistent radius, shadows, and hover/focus states.
Forms: consistent input styling, visible focus, no overlap issues with select dropdown.
Breadcrumbs readable and not overlapped by header.
Keyboard navigation: skip link works; focus order logical; all interactive items have visible focus.
No layout shifts on load; images have dimensions or reserved space.
Lighthouse (mobile): ≥90 Performance, ≥95 Accessibility, ≥95 Best Practices, ≥95 SEO.
Deliverables: updated styles.css, minimal HTML attribute tweaks, and small app.js additions (if any). No structural changes, no new dependencies.