Goal: Upgrade the visual polish of each service page header/hero (breadcrumb, back link, title, subcopy, divider) to a premium standard. Do not change routes, copy elsewhere, or global structure.
Guardrails
Do not alter section order, routing, filenames, or assets.
Only modify the service page hero block markup and append CSS to the main stylesheet.
Keep existing header/footer, forms, and content intact.
Files to touch
services/site-clear-outs.html
services/waste-removal.html
services/ground-preparation.html
services/haulage-support.html
public/styles.css (append styles)
1) Replace only the service hero block on each page
Find the current service hero section (top of each service page) and replace it with the block below, updating the current page title and subcopy per page:
<!-- Service Hero (polished) -->
<section class="svc-hero svc-hero--compact" aria-labelledby="svc-title">
  <div class="container">
    <nav class="svc-breadcrumb" aria-label="Breadcrumb">
      <a href="/">Home</a><span aria-hidden="true">/</span>
      <a href="/#services">Services</a><span aria-hidden="true">/</span>
      <span class="current">Site Clear-Outs</span>
    </nav>

    <a class="svc-back" href="/#services" aria-label="Back to services">
      <span class="svc-back__icon" aria-hidden="true"></span>
      Back to Services
    </a>

    <header class="svc-headline">
      <h1 id="svc-title" class="svc-title">Site Clear-Outs</h1>
      <p class="svc-sub">
        Fast, compliant site preparation with responsible waste handling and professional execution.
      </p>
    </header>

    <div class="svc-divider" aria-hidden="true"></div>
  </div>
</section>
Per page, update:
The last breadcrumb <span class="current">…</span>
<h1 class="svc-title">…</h1>
The <p class="svc-sub">…</p> sentence (keep length and tone)
2) Append the following CSS to public/styles.css
(Do not remove existing styles; append.)
:root{
  --orange:#F15A29; --ink:#0B0B0B; --charcoal:#121212; --muted:#B9B9B9; --white:#fff;
  --radius-1:10px; --radius-2:14px;
  --border-1:1px solid rgba(255,255,255,.08);
  --shadow-1:0 10px 28px rgba(0,0,0,.28);
  --t: .22s cubic-bezier(.2,.8,.2,1);
}

/* Service hero container */
.svc-hero{ background:#111; color:#fff; }
.svc-hero--compact{ padding: calc(var(--header-h,64px) + 28px) 0 28px; }
.svc-hero .container{ max-width:1100px; margin:0 auto; padding:0 20px; }

/* Breadcrumb */
.svc-breadcrumb{ display:flex; gap:8px; align-items:center; color:#C9C9C9; font-size:14px; margin:0 0 10px; }
.svc-breadcrumb a{ color:#fff; text-decoration:none; opacity:.9; transition:opacity var(--t); }
.svc-breadcrumb a:hover{ opacity:1; text-decoration:underline; }
.svc-breadcrumb .current{ color:#fff; opacity:.85; }

/* Back link */
.svc-back{ display:inline-flex; align-items:center; gap:8px; margin:6px 0 14px;
  color:var(--orange); text-decoration:none; font-weight:700; }
.svc-back:hover .svc-back__icon{ transform:translateX(-2px); }
.svc-back__icon{ width:18px; height:18px; flex:0 0 18px; border-left:2px solid var(--orange);
  border-bottom:2px solid var(--orange); transform:rotate(45deg); transition:transform var(--t); }

/* Headline block */
.svc-headline{ max-width:860px; }
.svc-title{ margin:.25rem 0 .35rem; font-size:clamp(32px,5vw,56px); line-height:1.06; letter-spacing:.2px; }
.svc-sub{ margin:0; color:#D0D0D0; font-size:clamp(16px,1.8vw,20px); }

/* Divider */
.svc-divider{ height:1px; margin:22px 0 0;
  background:linear-gradient(90deg,transparent, rgba(255,255,255,.10), transparent); }

/* Respect sticky header when navigating within page */
.svc-hero, .svc-hero * { scroll-margin-top: var(--header-h,64px); }

/* Small screens */
@media (max-width: 720px){
  .svc-breadcrumb{ font-size:13px; }
  .svc-back{ margin:4px 0 12px; }
}
3) Keep header behavior and spacing intact
Do not change the sticky header logic.
Ensure there is no autofocus on lower fields that could scroll the page.
Ensure no hash fragments (#...) in the “Learn more” links—should point to the actual .html pages.
4) QA / Acceptance (agent must self-check before finishing)
Each service page shows: breadcrumb → back link → H1 → subcopy → subtle divider.
Header does not overlap the breadcrumb/title in any viewport.
Spacing and typography match across all four pages.
No console errors; Lighthouse Accessibility ≥95 on a service page.
Mobile: breadcrumb wraps gracefully; headline scales with clamp().
Deliverable: Updated hero block in each service page + appended CSS. No other sections changed.