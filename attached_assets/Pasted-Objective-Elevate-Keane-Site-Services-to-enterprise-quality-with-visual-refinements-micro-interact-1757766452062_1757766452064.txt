Objective: Elevate Keane Site Services to enterprise quality with visual refinements, micro-interactions, accessibility, and performance tuning — without changing how the site functions or its structure.
Guardrails (do not violate)
Do not change layout order, routes, IDs, copy, images, or component names.
Preserve the header behavior (transparent at top → dark on scroll) and logo swap.
Keep the hero video behavior intact.
Scope CSS additions to new utility/classes or existing selectors; do not remove existing rules unless they are an obvious bug (comment them instead).
If any step could cause risk, skip it and report what you skipped.
1) Design Tokens & Rhythm (styles only; no DOM changes)
Task: Centralize colors, radii, shadows, timing, and spacing so everything feels unified.
Add/extend tokens at the top of the main stylesheet:
:root{
  --orange:#F15A29; --white:#fff; --ink:#0B0B0B; --charcoal:#121212; --muted:#B8B8B8;
  --container-w:1100px; --gutter:20px; --header-h:64px;
  --radius-1:10px; --radius-2:14px; --radius-3:18px; --pill:999px;
  --shadow-1:0 6px 18px rgba(0,0,0,.25);
  --shadow-2:0 10px 28px rgba(0,0,0,.35);
  --ease:.22s cubic-bezier(.2,.8,.2,1);
}
Normalize base spacing/typo (no size changes, just consistency):
.container{max-width:var(--container-w); margin-inline:auto; padding-inline:var(--gutter);}
h1{line-height:1.06; letter-spacing:.1px}
h2{line-height:1.12}
section{padding-block:clamp(44px,6vw,72px)}
2) Buttons & Links — premium hover/focus (no layout change)
.btn{border-radius:var(--radius-1); padding:.78rem 1.05rem; font-weight:700;
     border:1px solid transparent; transition:transform var(--ease), box-shadow var(--ease), border-color var(--ease)}
.btn--primary{background:var(--orange); color:#111; box-shadow:var(--shadow-1)}
.btn--primary:hover{transform:translateY(-1px); box-shadow:var(--shadow-2)}
.btn--ghost{background:transparent; color:var(--white); border-color:rgba(255,255,255,.5)}
.btn:focus-visible{outline:0; box-shadow:0 0 0 3px rgba(241,90,41,.35)}
a.link-underline{position:relative; text-decoration:none}
a.link-underline::after{content:""; position:absolute; left:0; right:100%; bottom:-2px; height:2px; background:var(--orange); transition:right var(--ease)}
a.link-underline:hover::after{right:0}
3) Cards & Surfaces — consistent elevation
.card,.svc-card,.strength{
  border:1px solid rgba(255,255,255,.08);
  border-radius:var(--radius-2);
  background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.02));
  transition:transform var(--ease), box-shadow var(--ease), border-color var(--ease), background var(--ease)
}
.card:hover,.svc-card:hover,.strength:hover{transform:translateY(-2px); box-shadow:var(--shadow-1); border-color:rgba(241,90,41,.35)}
4) Motion Hygiene (subtle + accessible; no functional change)
Add a gentle fade-in for hero copy and section headers; respect reduced motion.
.fade-in{opacity:0; transform:translateY(6px); animation:fadeIn .5s var(--ease) forwards}
@keyframes fadeIn{to{opacity:1; transform:none}}
@media (prefers-reduced-motion:reduce){.fade-in{animation:none; opacity:1; transform:none}}
Apply .fade-in to hero headings/lead and key section titles without changing their order.
5) Header/Toolbar — parity + polish (keep existing behavior)
Do not change the logic; just ensure quality:
.site-header{position:fixed; top:0; left:0; right:0; height:var(--header-h); z-index:1000;
  background:transparent; transition:background .25s ease, box-shadow .25s ease, border-color .25s ease}
.scrolled .site-header{background:rgba(0,0,0,.90); backdrop-filter:saturate(140%) blur(8px);
  box-shadow:0 6px 18px rgba(0,0,0,.28); border-bottom:1px solid rgba(255,255,255,.06)}
.site-header .wrap{height:100%; display:flex; align-items:center; justify-content:space-between; gap:16px;
  padding-inline:var(--gutter); max-width:var(--container-w); margin-inline:auto}
.nav a{color:#fff; text-decoration:none; margin-left:18px; font-weight:600; opacity:.92; transition:opacity var(--ease)}
.nav a:hover{opacity:1}
Bug guard: remove any stray 1px lines under header on Services:
.page--services .site-header{border-bottom:0 !important; box-shadow:none;}
6) “Why Choose Keane Site Services” — micro-interaction only
Confirm heading reads “Why Choose Keane Site Services” (text only).
Add tasteful hover to the four reasons (no layout shifts):
.why .card:hover{transform:translateY(-2px); box-shadow:var(--shadow-2); border-color:rgba(241,90,41,.35)}
.why .card .icon{filter:drop-shadow(0 2px 6px rgba(0,0,0,.2))}
7) Form UX — dropdown clarity + focus (no structure change)
Ensure the “Select a service” dropdown is solid (not transparent), with strong focus:
.form select, .contact .form select, select[name="service"]{
  background:#fff !important; color:#111; border:1px solid rgba(0,0,0,.2); border-radius:var(--radius-1);
  appearance:none;
  background-image:linear-gradient(45deg,transparent 50%, #111 50%),linear-gradient(135deg,#111 50%,transparent 50%);
  background-position:calc(100% - 18px) calc(1.05em),calc(100% - 13px) calc(1.05em);
  background-size:5px 5px,5px 5px; background-repeat:no-repeat; z-index:5;
}
.form select:focus{outline:0; box-shadow:0 0 0 3px rgba(241,90,41,.25); border-color:rgba(241,90,41,.55)}
.form,.form-grid,.form-group{overflow:visible !important} /* prevent dropdown clipping */
8) Services Pages — alignment & top landing (no copy changes)
Body has class="page--services" (already present per prior tasks).
Ensure hero (breadcrumb/back/H1/sub) left-aligns with body:
.page--services .container{max-width:var(--container-w); padding-inline:var(--gutter); margin-inline:auto}
.page--services .svc-hero{background:#111; color:#fff}
.page--services .svc-hero--compact{padding:calc(var(--header-h) + 28px) 0 28px}
.page--services .svc-hero .container{max-width:var(--container-w); padding-inline:var(--gutter)}
.page--services .svc-breadcrumb,.page--services .svc-back,.page--services .svc-headline,.page--services .svc-divider{margin-left:0 !important}
.page--services .svc-main .container{max-width:var(--container-w); padding-inline:var(--gutter)}
Force top-of-page landing (no mid-scroll):
// Keep header behavior + ensure service pages open at top
(function(){
  if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
  window.addEventListener('pageshow', () => {
    try{window.scrollTo({top:0,left:0,behavior:'instant'});}catch{window.scrollTo(0,0);}
  });
  if(location.hash && !location.hash.startsWith('#top')){
    history.replaceState(null,'',location.pathname+location.search);
  }
})();
9) Performance Upgrades (safe only; no visual change)
Video pipeline (non-destructive):
Confirm hero MP4 is H.264 yuv420p, -movflags +faststart, and scaled to 1920 width (keep aspect).
Ensure there’s a lightweight poster.jpg (first second) to avoid black flash.
Do not change the file name/path currently used; only re-encode in place if required.
Images:
For each <img>, add width/height attributes (actual intrinsic sizes) to prevent CLS.
Add loading="lazy" to all non-hero, below-the-fold images.
Where safe, generate lightweight WebP alternates and use:
<picture>
  <source srcset="path.webp" type="image/webp">
  <img src="path.png" width="W" height="H" alt="..." loading="lazy">
</picture>
(If any image is used as a CSS background or logo, leave it unchanged.)
Fonts & CSS/JS critical path:
Add:
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<meta name="theme-color" content="#111111">
Ensure all custom scripts use defer.
Keep a single stylesheet; if there are two, mark non-critical as media="print" + onload swap only if it doesn’t flicker (otherwise keep as is).
Caching hints (static hosting safe):
Where supported in the hosting context, set far-future cache headers for assets in /assets/* and short cache for HTML. If not configurable here, skip and note.
10) Accessibility & Semantics (zero redesign)
Ensure a skip link exists and is visible on focus:
<a class="skip-link" href="#main">Skip to content</a>
.skip-link{position:absolute;left:-9999px;top:auto}
.skip-link:focus{left:20px;top:14px;background:#000;color:#fff;padding:.5rem .7rem;border-radius:10px;z-index:1100}
All actionable elements have a visible focus (already covered in btn/link focus).
Add aria-current="page" to the active nav item per page (safe attribute).
Forms: ensure every input has a <label>; add autocomplete, inputmode, and aria-live="polite" for form status messages.
11) Metadata & SEO (no content rewrite)
Verify each page has unique <title> and <meta name="description">.
Maintain Service JSON-LD where added; ensure provider.name = "Keane Site Services" and areaServed:"IE".
12) QA & Acceptance (must pass before finishing)
Visual: No layout shifts; header behaves exactly as before; hero video loads with poster; cards/buttons have subtle premium hover; “Why Choose Keane Site Services” present.
Alignment: Service hero left edge matches Overview/What’s Included; no header bottom line on services.
Forms: Service dropdown is solid (not transparent); focus rings present; dropdown not clipped.
Performance:
CLS ≤ 0.01 on Home and a Service page.
LCP image/video has proper attributes; fonts not blocking render.
All non-critical images lazy-loaded.
Accessibility: Keyboard navigation works; skip link visible on focus; active nav uses aria-current.
No console errors.
If any recommended step was skipped for safety, list it at the end with reason.
